# ReadMe 
> Было бы неплохо написать, для понту 
```
├── README.md          <- The top-level README for developers using this project.
│
├── fastapi            <- FastAPI application
├── models             <- Trained and serialized models, model predictions, or model summaries
│
├── src                <- Source code
│   └── predictions    <- do and save predictions
├── static             <- Assets for docs (if needed)
└─── streamlit_app     <- Streamlit application
    ├── .streamlit     <- Streamlit configs see: https://docs.streamlit.io/library/advanced-features/configuration
    ├── static         <- Assets for app
    └── pages          <- streamlit info pages
```